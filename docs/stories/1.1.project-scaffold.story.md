# Story 1.1: Project Scaffold & HTML Structure

## Status
Done

## Executor Assignment
```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["manual-review", "browser-console-check"]
```

## Story
**As a** desenvolvedor,
**I want** criar a estrutura base do projeto com HTML semântico e grid de botões,
**so that** a calculadora tenha uma fundação organizada pronta para lógica e estilo.

## Acceptance Criteria
1. Criar os 3 arquivos: `index.html`, `style.css`, `script.js` com links corretamente configurados
2. O `index.html` contém um elemento `<div class="calculator">` com display e grid de botões
3. O display possui dois elementos: expressão atual (`#expression`) e resultado (`#result`)
4. O grid contém todos os 19 botões: AC, +/−, %, ÷, 7, 8, 9, ×, 4, 5, 6, −, 1, 2, 3, +, 0, ., =
5. Cada botão possui `data-action` e `data-value` attributes para seleção via JS
6. O `style.css` inclui reset básico e `box-sizing: border-box`
7. O `script.js` é linkado no final do `<body>` e não gera erros no console

## 🤖 CodeRabbit Integration

**Story Type Analysis**
- **Primary Type**: Frontend
- **Secondary Type(s)**: N/A
- **Complexity**: Low — estrutura inicial, único arquivo HTML, sem lógica

**Specialized Agent Assignment**
- **Primary Agents**:
  - @dev (implementação e pre-commit review)
- **Supporting Agents**:
  - @qa (validação manual no browser)

**Quality Gate Tasks**
- [ ] Pre-Commit (@dev): Verificar HTML válido, links corretos, sem erros no console
- [ ] Pre-PR (@devops): Confirmar estrutura de arquivos antes de push

**Self-Healing Configuration**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**CodeRabbit Focus Areas**
- **Primary Focus**:
  - Semântica HTML5 correta (uso de elementos apropriados)
  - Atributos `data-action` e `data-value` presentes em todos os botões
- **Secondary Focus**:
  - Links CSS/JS corretos e funcionais
  - Reset CSS básico incluído

## Tasks / Subtasks

- [x] Criar arquivo `index.html` com estrutura base (AC: 1, 2)
  - [x] Adicionar `<!DOCTYPE html>`, `<head>` com charset, viewport e links
  - [x] Criar `<div class="calculator-wrapper">` para os orbs + calculadora
  - [x] Criar `<div class="calculator">` com `<div class="display">` e `<div class="buttons">`
- [x] Adicionar elementos do display (AC: 3)
  - [x] `<div id="expression">` para expressão atual
  - [x] `<div id="result">` para o valor/resultado
- [x] Criar grid de botões completo (AC: 4, 5)
  - [x] Linha 1: AC (`data-action="clear"`), +/− (`data-action="sign"`), % (`data-action="percent"`), ÷ (`data-action="operator" data-value="/"`)
  - [x] Linha 2: 7, 8, 9, × (`data-action="operator" data-value="*"`)
  - [x] Linha 3: 4, 5, 6, − (`data-action="operator" data-value="-"`)
  - [x] Linha 4: 1, 2, 3, + (`data-action="operator" data-value="+"`)
  - [x] Linha 5: 0 (span 2 colunas), . (`data-action="decimal"`), = (`data-action="equals"`)
  - [x] Dígitos com `data-action="digit"` e `data-value="{n}"`
- [x] Criar `style.css` com reset (AC: 6)
  - [x] `* { margin: 0; padding: 0; box-sizing: border-box; }`
- [x] Criar `script.js` vazio com comentário de estrutura (AC: 7)
  - [x] Linkar no final do `<body>` com `<script src="script.js"></script>`
- [x] Validar no browser: abrir `index.html` e verificar console sem erros (AC: 7)

## Dev Notes

**Stack:** HTML5 puro, CSS3, Vanilla JS — zero dependências externas. Nenhum framework ou build tool.

**Estrutura de arquivos esperada:**
```
Teste Calculadora/
├── index.html
├── style.css
└── script.js
```

**Convenção de atributos dos botões:**
- `data-action`: tipo de ação — `"digit"`, `"operator"`, `"clear"`, `"sign"`, `"percent"`, `"decimal"`, `"equals"`, `"backspace"`
- `data-value`: valor associado — número ou símbolo do operador (`"+"`, `"-"`, `"*"`, `"/"`)

**Layout do grid (4 colunas):**
```
[ AC ] [ +/- ] [ %  ] [ ÷  ]
[  7 ] [  8  ] [  9 ] [ ×  ]
[  4 ] [  5  ] [  6 ] [ -  ]
[  1 ] [  2  ] [  3 ] [ +  ]
[    0    ]   [ .  ] [ =  ]
```
O botão `0` usa `grid-column: span 2` (será aplicado no CSS do Epic 2).

**Referência PRD:** FR1, FR2 — `docs/prd.md`

### Testing
- Abrir `index.html` diretamente no browser (Chrome/Firefox/Edge)
- Verificar aba Console: zero erros
- Inspecionar DOM e confirmar todos os 19 botões com atributos corretos
- Contar botões: 4 + 4 + 4 + 4 + 3 = 19 ✓

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-07 | 1.0 | Story criada | River (@sm) |
| 2026-05-07 | 1.1 | Validado GO (7.5/10) — Status: Draft → Ready | @po |
| 2026-05-07 | 1.2 | Implementação completa — todos os ACs verificados | @dev |

## Dev Agent Record

### Agent Model Used
Claude Sonnet (Dex — @dev)

### Debug Log References
Nenhum — implementação direta sem erros.

### Completion Notes
- 3 arquivos criados: `index.html`, `style.css`, `script.js`
- 19 botões com `data-action` + `data-value` corretos: 10 dígitos, 4 operadores, 1 clear, 1 sign, 1 percent, 1 decimal, 1 equals
- CSS inclui reset universal + layout base + grid 4 colunas + `.btn-zero` span 2
- `script.js` contém estrutura comentada e `console.log` de confirmação
- Todos os 7 ACs verificados via grep/bash — 19 `data-action` confirmados

### File List
- `index.html` — CRIADO
- `style.css` — CRIADO
- `script.js` — CRIADO

## QA Results

**Revisor:** Quinn (@qa) | **Data:** 2026-05-07 | **Verdict:** ✅ PASS

### Gate Decision: PASS

| Check | Status | Detalhe |
|-------|--------|---------|
| #result presente no DOM | ✅ PASS | Elemento existe e acessível |
| #expression presente no DOM | ✅ PASS | Elemento existe e acessível |
| Mínimo 19 botões com data-action | ✅ PASS | 20 botões detectados (inclui ⌫) |
| data-action="digit" presente | ✅ PASS | 10 botões de dígito |
| data-action="operator" presente | ✅ PASS | 4 operadores |
| data-action="equals" presente | ✅ PASS | 1 botão = |
| Zero erros no console | ✅ PASS | 0 errors, 0 warnings |
| script.js linkado sem erros | ✅ PASS | "script.js carregado ✓" no console |

**Observações:** Estrutura HTML semântica correta. Botão ⌫ adicionado como 20º botão no display — melhoria justificada pelo AC8 da Story 1.2. Todos os 7 ACs originais satisfeitos.
